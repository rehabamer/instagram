#instagram authentication

install.packages('instaR')
library(instaR)
require(httr)
library(httr)
require(rjson)
library("rjson")
library("bitops")
require(RCurl)
library("RCurl")

full_url <- oauth_callback()
full_url <- gsub("(.*localhost:[0-9]{1,5}/).*", x=full_url, replacement="\\1")
message <- paste("Copy and paste into Site URL on Instagram App Settings:", 
                 full_url, "\nWhen done, press any key to continue...")
invisible(readline(message))
app_name <- "xxxxxxx"
client_id <- "xxxxxxx"
client_secret <- "xxxxxxxxxxx"
scope = "public_content"
instagram <- oauth_endpoint(
  authorize = "https://api.instagram.com/oauth/authorize",
  access = "https://api.instagram.com/oauth/access_token")  
myapp <- oauth_app(app_name, client_id, client_secret)
ig_oauth<- instaOAuth(client_id, client_secret, scope = c("public_content"))


# get user funcation


instaOAuth(client_id, client_secret, scope = c("follower_list"))
ruby <- getFollowers( username="ruby_amer88", token=ig_oauth )

# get user media funcation

Eian <- getUserMedia(username = "eian_foad" , token = ig_oauth , folder = "Eian")
